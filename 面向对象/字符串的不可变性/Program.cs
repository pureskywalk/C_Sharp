using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace 字符串的不可变性
{
    class Program
    {
        static void Main(string[] args)
        {
            //给一个string变量重新赋值并不会覆盖原来堆上的位置，而是重新开辟一块新的堆空间
        }
    }
}
